services:
  ambient-mixer:
    build: ../app
    ports:
      - "8000:8000"
    volumes:
      - ./sounds:/app/sounds:ro
      - ./scenes:/app/scenes:ro
    env_file:
      - .env
    restart: unless-stopped
